# Terraform-Azure-Linux-VMs-Creator
This Terraform script configures resources in Azure, such as a resource group, virtual network, subnet, public IP addresses, network interfaces, network security group, and virtual machines. The created resources are associated and configured based on specified parameters.
